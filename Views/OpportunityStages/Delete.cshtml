@model OpenLawOffice.Web.ViewModels.Opportunities.OpportunityStageViewModel

@{
    Layout = "~/Views/Shared/_NewLayout.cshtml";
    ViewBag.Title = "Delete Opportunity Stage";
    ViewBag.PageTitle = "Delete Opportunity Stage";
}

@section Breadcrumb {
    <ul class="breadcrumb">
        <li><a href="/Settings">Settings</a></li>
        <li><a href="/OpportunityStage">Opportunity Stages</a></li>
        <li>Opportunity Stage: <a href="/OpportunityStage/Details/@(Model.Id)">@(Model.Title)</a></li>
        <li><a href="/OpportunityStage/Delete/@(Model.Id)" class="active">Delete</a></li>
    </ul>
}

@using (Html.BeginForm())
{
    @(Html.ValidationSummary(true))
    <h3>
        Are you sure you want to disable this?
    </h3>

    <div class="panel panel-default">
        <div class="panel-heading">Opportunity Stage Information</div>
        <div class="panel-body">
            <div class="form-group">
                <span class="control-label col-sm-2">Title<span class="required-field" title="Required Field">*</span></span>
                <div class="col-sm-10" style="padding-top: 7px;">
                    @(Model.Title)
                </div>
            </div>
            <div class="form-group">
                <span class="control-label col-sm-2">Order<span class="required-field" title="Required Field">*</span></span>
                <div class="col-sm-10" style="padding-top: 7px;">
                    @(Model.Order)
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-default">Submit</button>
                </div>
            </div>
        </div>
    </div>
}